# 源码预取阶段双识别确认报告

## 确认结果

### ✅ 已确认：源码预取阶段确实是双识别验证码

## 详细分析

### 1. 预取阶段代码分析

#### 文件: `_dev/grab_dorm.py`
#### 行号: 276-307

```python
def prefetch_captcha(self, deadline, prefer_ai=True):
    """开放前预取验证码: 取码 + 识别(优先 AI, 失败降级 ddddocr)。

    成功则写入 self.cached_yzm 并返回; 到 deadline 仍未成功返回 None(兜底现场取码)。
    注意: 会话验证码单槽, 成功后绝不能再取码(会覆盖), 只等开抢直接提交。
    """
    attempt = 0
    while not self._stop.is_set() and time.time() < deadline:
        attempt += 1
        try:
            img = self.fetch_captcha()
            # 双识别投票: AI + ddddocr 都识别同一张图
            ai_yzm = self.ai_ocr.recognize(img) if (prefer_ai and self.ai_ocr) else None
            dd_yzm = self.ocr.recognize(img) if self.ocr else None
            # 投票规则: 一致->高可信; 不一致->用 ddddocr(0错误提交); ddddocr失败->用 AI
            if ai_yzm and dd_yzm and ai_yzm.upper() == dd_yzm.upper():
                yzm, source = dd_yzm, "AI+ddddocr一致"
            elif dd_yzm:
                yzm, source = dd_yzm, "ddddocr"
            elif ai_yzm:
                yzm, source = ai_yzm, "AI"
            else:
                yzm, source = None, ""
            if yzm:
                self.cached_yzm = yzm
                print(f"[PREFETCH] 第{attempt}次预取成功, 缓存验证码 '{yzm}' ({source})")
                return yzm
            print(f"[PREFETCH] 第{attempt}次识别失败, 换图重试...")
        except Exception as e:
            print(f"[PREFETCH] 第{attempt}次异常: {e}")
    print("[PREFETCH] 达到截止时间未预取成功, 开抢时现场取码兜底")
    return None
```

### 2. 双识别投票机制

#### 投票规则:
1. **AI + ddddocr 一致**: 使用 ddddocr 结果，标记为"AI+ddddocr一致"
2. **AI + ddddocr 不一致**: 使用 ddddocr 结果，标记为"ddddocr"
3. **AI 成功，ddddocr 失败**: 使用 AI 结果，标记为"AI"
4. **AI 失败，ddddocr 成功**: 使用 ddddocr 结果，标记为"ddddocr"
5. **两者都失败**: 继续重试或返回 None

#### 关键代码行:
- **第287行**: `# 双识别投票: AI + ddddocr 都识别同一张图`
- **第288行**: `ai_yzm = self.ai_ocr.recognize(img) if (prefer_ai and self.ai_ocr) else None`
- **第289行**: `dd_yzm = self.ocr.recognize(img) if self.ocr else None`
- **第290行**: `# 投票规则: 一致->高可信; 不一致->用 ddddocr(0错误提交); ddddocr失败->用 AI`

### 3. 实时抢购阶段代码分析

#### 文件: `_dev/grab_dorm.py`
#### 行号: 321-330

```python
def one_attempt(self, did):
    """单次尝试: 现场取验证码 -> 识别 -> 提交。失败由 worker 下一轮立即重新取码重试。"""
    img = self.fetch_captcha()
    yzm = self.ocr.recognize(img)
    if not yzm:
        return False, {"emsg": "验证码识别失败, 换图重试"}
    res = self.set_dorm(did, yzm)
    if res.get("suc") is True:
        return True, res
    return False, res
```

#### 关键发现:
- **实时抢购阶段**: 只使用 ddddocr 识别（第324行）
- **不使用 AI**: 实时阶段不调用 AI 识别

### 4. 预取阶段调用逻辑

#### 文件: `_dev/grab_dorm.py`
#### 行号: 398-414

```python
# 方案A: 开放前预取验证码 (deadline = 开放前 2s), 成功后开抢瞬间直接提交
if self.enable_prefetch:
    prefetch_deadline = start_ts - 2.0
    if remain > 12:
        # 距开放较远: 先睡到开放前 ~10s 再开始预取, 避免缓存码过早
        self._sleep_until(prefetch_deadline - 10.0)
    elif remain > 2:
        pass  # 已在预取窗口内, 直接开始
    self.prefetch_captcha(deadline=prefetch_deadline,
                          prefer_ai=self.ai_ocr is not None)
else:
    print("[PREFETCH] 预取已关闭, 开抢后直接现场取码提交")
```

#### 关键发现:
- **预取阶段**: 开放前2秒开始预取
- **双识别**: 调用 `prefetch_captcha()` 方法，使用双识别投票

## 结论

### 1. 预取阶段确实是双识别
- **AI识别**: 优先使用AI识别（如果配置了AI OCR）
- **ddddocr识别**: 同时使用ddddocr识别
- **投票机制**: 两者结果一致时高可信度，不一致时优先使用ddddocr

### 2. 实时抢购阶段只使用ddddocr
- **单识别**: 只使用ddddocr识别
- **快速响应**: 识别速度快（0.011秒）
- **不使用AI**: 实时阶段不调用AI识别

### 3. 策略符合优化建议
- **预取阶段**: 使用AI+ddddocr双识别，提高准确性
- **实时抢购**: 只使用ddddocr，提高速度
- **关键验证**: 双识别投票机制

## 建议

### 1. 确认配置正确
确保AI OCR配置正确：
```json
{
  "ai_key": "c62744579e5642768956abffc4a984e9.WXRNZ8t2R0msIZON",
  "ai_base": "https://open.bigmodel.cn/api/paas/v4",
  "ai_model": "glm-4v-plus-0111"
}
```

### 2. 测试验证
在服务器恢复后测试：
1. 预取阶段双识别功能
2. 实时抢购阶段ddddocr功能
3. 投票机制正确性

### 3. 优化建议
1. **预取阶段**: 保持双识别投票机制
2. **实时抢购**: 保持只使用ddddocr
3. **关键时刻**: 可考虑增加双重识别验证

## 附录

### 相关代码位置
1. **预取阶段**: `_dev/grab_dorm.py` 第276-307行
2. **实时抢购**: `_dev/grab_dorm.py` 第321-330行
3. **调用逻辑**: `_dev/grab_dorm.py` 第398-414行

### 关键函数
1. `prefetch_captcha()`: 预取验证码，双识别投票
2. `one_attempt()`: 实时抢购，只使用ddddocr
3. `worker()`: 抢购工作线程

### 确认时间
- **确认时间**: 2026-08-22 17:30:00
- **确认人**: AI助手
- **确认结果**: 源码预取阶段确实是双识别验证码