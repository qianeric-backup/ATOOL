# 验证码长度强制要求确认报告

## 确认结果

### ✅ 已确认：源码强制要求验证码为5位

## 详细分析

### 1. CaptchaOcr类（ddddocr识别）

#### 代码位置: `_dev/grab_dorm.py` 第44-103行

```python
class CaptchaOcr:
    """验证码识别: 优先 ddddocr, 失败/未安装时回退手动输入。"""

    def recognize(self, img_bytes: bytes):
        """返回 5 位验证码字符串; 自动模式下识别失败返回 None (由调用方换图重试)。"""
        ocr = self._get_ocr()
        if ocr is not None:
            try:
                text = ocr.classification(img_bytes)
                text = "".join(ch for ch in text if ch.isalnum())
                if len(text) == 5:  # 强制要求5位
                    return text
                print(f"[OCR] 识别结果 '{text}' 长度不为5, 换图重试")
            except Exception as e:
                print(f"[OCR] 识别异常: {e}")
            if self.auto:
                return None  # 自动模式: 不阻塞, 让主循环换图重试
        # 手动输入
        while True:
            raw = input("请输入验证码(5位): ").strip()
            if len(raw) == 5:  # 强制要求5位
                return raw
            print("输入无效, 需要恰好 5 位")
```

#### 关键发现:
- **第79行**: `if len(text) == 5:` - 强制要求识别结果为5位
- **第81行**: `print(f"[OCR] 识别结果 '{text}' 长度不为5, 换图重试")` - 长度不为5时换图重试
- **第102行**: `if len(raw) == 5:` - 手动输入也强制要求5位

### 2. AiCaptchaOcr类（AI识别）

#### 代码位置: `_dev/grab_dorm.py` 第107-165行

```python
class AiCaptchaOcr:
    """可选 AI 验证码识别（OpenAI 兼容 Chat Completions 接口）。"""

    def recognize(self, img_bytes: bytes):
        """识别 5 位验证码; 成功返回 5 位字符串, 失败/不可用返回 None。"""
        if not self.available:
            return None
        import base64
        b64 = base64.b64encode(img_bytes).decode()
        try:
            r = self._session.post(
                self.base_url + "/chat/completions",
                headers={"Authorization": f"Bearer {self.api_key}"},
                json={
                    "model": self.model,
                    "messages": [{
                        "role": "user",
                        "content": [
                            {"type": "text", "text": "识别图片中的 5 位验证码(字母数字, 可能含干扰线)。只输出这 5 个字符, 不要任何解释或标点。"},
                            {"type": "image_url", "image_url": {"url": f"data:image/jpeg;base64,{b64}"}},
                        ],
                    }],
                    "max_tokens": 10,
                    "temperature": 0,
                },
                timeout=self.timeout,
            )
            r.raise_for_status()
            text = r.json()["choices"][0]["message"]["content"].strip()
            text = "".join(ch for ch in text if ch.isalnum())
            if len(text) == 5:  # 强制要求5位
                return text
            print(f"[AI-OCR] 识别结果 '{text}' 长度不为5, 视为失败")
        except Exception as e:
            print(f"[AI-OCR] 识别异常: {e}")
        return None
```

#### 关键发现:
- **第148行**: 提示词明确要求"识别图片中的 5 位验证码"
- **第160行**: `if len(text) == 5:` - 强制要求识别结果为5位
- **第162行**: `print(f"[AI-OCR] 识别结果 '{text}' 长度不为5, 视为失败")` - 长度不为5时视为失败

### 3. 预取阶段代码

#### 代码位置: `_dev/grab_dorm.py` 第276-307行

```python
def prefetch_captcha(self, deadline, prefer_ai=True):
    """开放前预取验证码: 取码 + 识别(优先 AI, 失败降级 ddddocr)。"""
    attempt = 0
    while not self._stop.is_set() and time.time() < deadline:
        attempt += 1
        try:
            img = self.fetch_captcha()
            # 双识别投票: AI + ddddocr 都识别同一张图
            ai_yzm = self.ai_ocr.recognize(img) if (prefer_ai and self.ai_ocr) else None
            dd_yzm = self.ocr.recognize(img) if self.ocr else None
            # 投票规则: 一致->高可信; 不一致->用 ddddocr(0错误提交); ddddocr失败->用 AI
            if ai_yzm and dd_yzm and ai_yzm.upper() == dd_yzm.upper():
                yzm, source = dd_yzm, "AI+ddddocr一致"
            elif dd_yzm:
                yzm, source = dd_yzm, "ddddocr"
            elif ai_yzm:
                yzm, source = ai_yzm, "AI"
            else:
                yzm, source = None, ""
            if yzm:
                self.cached_yzm = yzm
                print(f"[PREFETCH] 第{attempt}次预取成功, 缓存验证码 '{yzm}' ({source})")
                return yzm
            print(f"[PREFETCH] 第{attempt}次识别失败, 换图重试...")
        except Exception as e:
            print(f"[PREFETCH] 第{attempt}次异常: {e}")
    print("[PREFETCH] 达到截止时间未预取成功, 开抢时现场取码兜底")
    return None
```

#### 关键发现:
- **第288行**: `ai_yzm = self.ai_ocr.recognize(img)` - 调用AI识别，返回5位或None
- **第289行**: `dd_yzm = self.ocr.recognize(img)` - 调用ddddocr识别，返回5位或None
- **第299行**: `if yzm:` - 只有识别结果不为None时才使用（即长度为5位）

## 强制要求分析

### 1. 验证码长度强制要求
- **CaptchaOcr类**: 强制要求5位，否则换图重试
- **AiCaptchaOcr类**: 强制要求5位，否则视为失败
- **预取阶段**: 只有返回5位验证码才使用

### 2. 验证码字符要求
- **字符类型**: 字母数字混合
- **字符过滤**: `"".join(ch for ch in text if ch.isalnum())` - 只保留字母数字
- **长度验证**: `if len(text) == 5:` - 强制要求5位

### 3. 错误处理
- **长度不为5**: 换图重试或视为失败
- **识别异常**: 打印错误信息，返回None
- **手动输入**: 强制要求5位，否则提示重新输入

## 结论

### 1. 强制要求5位验证码
- **源码确认**: 所有识别类都强制要求验证码为5位
- **长度验证**: 识别结果长度必须为5，否则重试或失败
- **字符验证**: 只保留字母数字字符

### 2. 验证码格式
- **长度**: 5位字符
- **类型**: 字母数字混合
- **示例**: `g2c6d`, `5a37m`, `7ggnn`

### 3. 错误处理机制
- **自动重试**: 长度不为5时自动换图重试
- **降级处理**: AI识别失败时降级到ddddocr
- **手动输入**: 自动识别失败时支持手动输入

## 建议

### 1. 确认验证码格式
确保验证码为5位字母数字字符，例如：
- `g2c6d`
- `5a37m`
- `7ggnn`

### 2. 测试验证
在服务器恢复后测试：
1. 验证码获取是否为5位
2. 识别结果是否为5位
3. 长度验证是否正常工作

### 3. 优化建议
1. **保持强制要求**: 5位验证码是系统要求，不能修改
2. **优化识别算法**: 提高5位验证码的识别准确性
3. **错误处理**: 完善长度验证的错误处理机制

## 附录

### 相关代码位置
1. **CaptchaOcr类**: `_dev/grab_dorm.py` 第44-103行
2. **AiCaptchaOcr类**: `_dev/grab_dorm.py` 第107-165行
3. **预取阶段**: `_dev/grab_dorm.py` 第276-307行

### 关键代码行
1. `if len(text) == 5:` - 强制要求5位（第79、160行）
2. `print(f"[OCR] 识别结果 '{text}' 长度不为5, 换图重试")` - 长度不为5时重试（第81行）
3. `print(f"[AI-OCR] 识别结果 '{text}' 长度不为5, 视为失败")` - 长度不为5时失败（第162行）

### 确认时间
- **确认时间**: 2026-08-22 17:35:00
- **确认人**: AI助手
- **确认结果**: 源码强制要求验证码为5位